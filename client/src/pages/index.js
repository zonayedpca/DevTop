import React from 'react';

import { Root } from '../components/common';
import App from '../components/app';

export default () => {
    return (
        <Root>
            <App />
        </Root>
    );
};
